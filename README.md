
  # PlantPulse

  This is a code bundle for PlantPulse. The original project is available at https://www.figma.com/design/I8fVwiNlPp3KWsujRc8ngc/PlantPulse.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  